# React + TypeScript + Vite + Webassmbly

The barebones level project for getting a frontend operational with webassmbly running (C++ variant) invoked via an mjs. Includes shell script to compile with em++.
